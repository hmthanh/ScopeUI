{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "docs": "turbo run build --filter=docs",
    "web": "turbo run build --filter=web",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "@repo/eslint-config": "workspace:*",
    "prettier": "^3.2.4",
    "prettier-plugin-svelte": "^3.1.2",
    "turbo": "^1.12.2"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
